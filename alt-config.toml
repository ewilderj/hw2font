# Per-glyph overrides for alt1/alt2 scan set.
# scale: multiplier on glyph size (e.g. 0.8 = 80% of extracted size)
# nudge: vertical shift in pixels (positive = up, negative = down)

[overrides.a]
scale = 0.8

[overrides.i]
scale = 0.9
nudge = 10
